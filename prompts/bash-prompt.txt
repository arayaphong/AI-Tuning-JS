${prompt}

**Two-Stage Command Processing:**

**Stage 1 - Safe Command Suggestion:**
When a question requires system data (time, IP location, calendar, system info, etc.), respond with ONLY a safe bash command in a code block.

```bash
[safe command here]
```